const input=document.querySelector('#listInput'),output=document.querySelector('#listOutput');const rows=()=>input.value.split(/\r?\n/).map(x=>x.trim()).filter(Boolean);
const ops={az:a=>a.sort((x,y)=>x.localeCompare(y,'ro',{sensitivity:'base'})),za:a=>a.sort((x,y)=>y.localeCompare(x,'ro',{sensitivity:'base'})),unique:a=>[...new Map(a.map(x=>[x.toLocaleLowerCase('ro'),x])).values()],reverse:a=>a.reverse(),shuffle:a=>a.sort(()=>Math.random()-.5),number:a=>a.map((x,i)=>`${i+1}. ${x}`),clean:a=>a.map(x=>x.replace(/\s+/g,' ')),comma:a=>[a.join(', ')]};
document.querySelectorAll('[data-op]').forEach(b=>b.onclick=()=>output.value=ops[b.dataset.op](rows()).join('\n'));document.querySelector('#compare').onclick=()=>{const a=rows(),b=document.querySelector('#secondList').value.split(/\r?\n/).map(x=>x.trim()).filter(Boolean),low=x=>x.toLocaleLowerCase('ro');output.value=`Doar în prima listă:\n${a.filter(x=>!b.some(y=>low(y)===low(x))).join('\n')||'—'}\n\nDoar în a doua listă:\n${b.filter(x=>!a.some(y=>low(y)===low(x))).join('\n')||'—'}`};ops.clean(rows());output.value=ops.clean(rows()).join('\n');
