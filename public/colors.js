const picker=document.querySelector('#picker'),hex=document.querySelector('#hex');function rgbOf(h){h=h.replace('#','');return[parseInt(h.slice(0,2),16),parseInt(h.slice(2,4),16),parseInt(h.slice(4,6),16)]}function hexOf(r,g,b){return'#'+[r,g,b].map(x=>Math.max(0,Math.min(255,x)).toString(16).padStart(2,'0')).join('')}
function update(){let h=hex.value.trim();if(!/^#[0-9a-f]{6}$/i.test(h))return;h=h.toUpperCase();picker.value=h.toLowerCase();const[r,g,b]=rgbOf(h),mx=Math.max(r,g,b)/255,mn=Math.min(r,g,b)/255,d=mx-mn,l=(mx+mn)/2;let hue=0,s=0;if(d){s=d/(1-Math.abs(2*l-1));hue=mx===r/255?60*(((g-b)/255/d)%6):mx===g/255?60*((b-r)/255/d+2):60*((r-g)/255/d+4)}hue=(hue+360)%360;const k=1-mx,c=k===1?0:(1-r/255-k)/(1-k),m=k===1?0:(1-g/255-k)/(1-k),y=k===1?0:(1-b/255-k)/(1-k);document.querySelector('#rgb').value=`rgb(${r}, ${g}, ${b})`;document.querySelector('#hsl').value=`hsl(${Math.round(hue)}, ${Math.round(s*100)}%, ${Math.round(l*100)}%)`;document.querySelector('#cmyk').value=`${Math.round(c*100)}%, ${Math.round(m*100)}%, ${Math.round(y*100)}%, ${Math.round(k*100)}%`;const sw=document.querySelector('#swatch');sw.style.background=h;sw.textContent=h;const shades=[-.55,-.28,0,.28,.55].map(v=>hexOf(Math.round(r+(v>0?(255-r):r)*v),Math.round(g+(v>0?(255-g):g)*v),Math.round(b+(v>0?(255-b):b)*v)));document.querySelector('#palette').innerHTML=shades.map(x=>`<div style="background:${x};color:${rgbOf(x).reduce((a,b)=>a+b)<380?'#fff':'#111'}">${x.toUpperCase()}</div>`).join('');const lum=(.2126*r+.7152*g+.0722*b)/255,text=lum>.5?'#111':'#fff',ratio=(Math.max(lum,text==='#fff'?1:0)+.05)/(Math.min(lum,text==='#fff'?1:0)+.05);const con=document.querySelector('#contrast');con.style.background=h;con.style.color=text;document.querySelector('#contrastInfo').textContent=`Text ${text==='#fff'?'alb':'negru'} · contrast aproximativ ${ratio.toFixed(2)}:1`}
picker.oninput=()=>{hex.value=picker.value.toUpperCase();update()};hex.oninput=update;function gradient(){const code=`linear-gradient(135deg, ${document.querySelector('#gradA').value}, ${document.querySelector('#gradB').value})`;document.querySelector('#gradient').style.background=code;document.querySelector('#gradientCode').value=code}document.querySelector('#gradA').oninput=gradient;document.querySelector('#gradB').oninput=gradient;update();gradient();
