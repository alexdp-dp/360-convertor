const faces=document.querySelector('#faces'),count=document.querySelector('#diceCount'),stage=document.querySelector('#diceStage'),history=[];
faces.addEventListener('input',()=>document.querySelector('#facesLabel').textContent=faces.value);count.addEventListener('input',()=>document.querySelector('#diceLabel').textContent=count.value);
document.querySelector('#roll').addEventListener('click',()=>{const values=Array.from({length:Number(count.value)},()=>Math.floor(Math.random()*Number(faces.value))+1);stage.innerHTML=values.map(v=>`<div class="die rolling">${v}</div>`).join('');const total=values.reduce((a,b)=>a+b,0)+Number(document.querySelector('#modifier').value||0);document.querySelector('#total').textContent=total;history.unshift(`D${faces.value}: ${values.join('+')} = ${total}`);history.splice(8);document.querySelector('#history').innerHTML=history.map(h=>`<span>${h}</span>`).join('')});
